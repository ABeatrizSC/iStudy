'use client'

import { CountdownTimer, Template, Title } from "@/components"

export default function Timer() {
    return (
        <Template>
            <Title>Timer</Title>
            <CountdownTimer />
        </Template>
    )
}